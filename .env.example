# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# --- Supabase ---
# Project: robinhood-personas (ref ufkaoyvdhicwdwbckutx), org Chibibytes, us-east-1
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_KEY=            # service-role key, SERVER-SIDE ONLY (bypasses RLS)

# --- Market data ---
FINNHUB_API_KEY=

# --- SEC EDGAR (no key, but they block generic user-agents) ---
SEC_USER_AGENT=Chibibytes Research jj@dulcenochemedia.com

# --- Report generation ---
ANTHROPIC_API_KEY=

# --- Ops ---
DISCORD_WEBHOOK_URL=             # failure alerts
